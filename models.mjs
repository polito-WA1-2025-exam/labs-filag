function InfoSize (size, nProteins, maxIngredients, initialPrice, maxQtyDaily) {
    this.size = size;
    this.nProteins = nProteins;
    this.maxIngredients = maxIngredients;
    this.initialPrice = initialPrice;
    this.maxQtyDaily = maxQtyDaily;
}

